language: python

jobs:
  include:
    - python: 3.5
    - python: 3.6
    - python: 3.7
    - python: 3.8
    - name: Python 3.8.0 on macOS
      os: osx
      language: shell

install:
  - ./.travis/install.sh

script:
  - ./.travis/test.sh

after_success:
  - codecov
